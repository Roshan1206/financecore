server:
  port: 8090
#  servlet:
#    context-path: /api

fc:
  auth:
    jwt:
      secret: 8LoEEq/i6RFn0E7Yzoe2W+CndT3Baoj0d4bPB17IGX8=
  config:
    database:
      schema: fc_auth
    security-resource: false
    auth:
      url: http://localhost:${server.port}
      client:
        id: user-service
        secret: user-service

spring:
  application:
    name: auth
  datasource:
    url: jdbc:postgresql://localhost:5432/finance_core?currentSchema=${fc.config.database.schema}
    username: postgres
    password: password
  jpa:
    properties:
      hibernate:
        default_schema: ${fc.config.database.schema}
    hibernate:
      ddl-auto: validate
  config:
    import: "optional:configserver:http://localhost:8888"
  security:
    user:
      name: fincancecore
      password: password
  profiles:
    active: oauth

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org:
      springframework:
        security: TRACE